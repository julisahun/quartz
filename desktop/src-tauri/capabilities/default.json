{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The app's own vault commands need no extra permissions; core defaults are enough.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
